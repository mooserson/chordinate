import React from 'react';
import PlaybackKeyboardContainer from './synth/playback_keyboard_container';
import DiscoverContainer from './sidebar/discover_container';

const Playback = () => (
  <div className="home">
    <PlaybackKeyboardContainer />
    <DiscoverContainer/ >
  </div>
);

export default Playback;
